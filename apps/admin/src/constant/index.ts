const JWT_TOKEN_NAME = 'kp-user-token';
const JWT_TOKEN_EXPIRY = 'kp-user-token-expiry';
const JWT_REFRESH_TOKEN_NAME = 'kp-user-refresh-token';
const REMEMBER_ME_COOKIE = 'kp-remember-me';
const KPAY_GQL_ENDPOINT = 'https://kp-pay.fly.dev/api/kp-pay';
const DEFAULT_ICON_SIZE = {
  DASHBOARD: '18'
};

export {
  JWT_TOKEN_EXPIRY,
  JWT_TOKEN_NAME,
  JWT_REFRESH_TOKEN_NAME,
  REMEMBER_ME_COOKIE,
  KPAY_GQL_ENDPOINT,
  DEFAULT_ICON_SIZE
};
