version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo 'Skipping dependency installation - deploying from pre-built dist files'
    build:
      commands:
        - echo 'Skipping build process - using committed dist files'
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
# customHeaders:
#   - pattern: '**/*'
#     headers:
#       - key: 'X-Frame-Options'
#         value: 'DENY'
#       - key: 'X-XSS-Protection'
#         value: '1; mode=block'

# redirects:
#   - source: '/<*>'
#     target: '/index.html'
#     status: '200'
#   - source: '/old-path'
#     target: '/new-path'
#     status: '301'
