    !/usr/bin/env sh
     . "$(dirname -- "$0")/_/husky.sh"

     echo "🚀 Pre-push checks disabled."

     Run build process (DISABLED)
     echo "🔨 Building project..."
     npm run build
     if [ $? -ne 0 ]; then
       echo "❌ Build failed. Please fix the issues before pushing."
       exit 1
     fi

     echo "✅ Pre-push checks skipped. Ready to push."
