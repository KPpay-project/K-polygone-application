import { TransferMoney } from '@/components/modules/transfer/transfer-money';

export const WithdrawalsMoney = () => {
  return <TransferMoney type="withdraw" />;
};

export default WithdrawalsMoney;
